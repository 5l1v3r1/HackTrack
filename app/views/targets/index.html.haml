%h1 Listing targets

%table
  %thead
    %tr
      %th Role
      %th Hostname
      %th Ip address
      %th Fqdn
      %th Project
      %th
      %th
      %th

  %tbody
    - @targets.each do |target|
      %tr
        %td= target.role
        %td= target.hostname
        %td= target.ip_address
        %td= target.fqdn
        %td= target.project_id
        %td= link_to 'Show', target
        %td= link_to 'Edit', edit_target_path(target)
        %td= link_to 'Destroy', target, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Target', new_target_path
